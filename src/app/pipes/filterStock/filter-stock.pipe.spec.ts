import { FilterStockPipe } from './filter-stock.pipe';

describe('FilterStockPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStockPipe();
    expect(pipe).toBeTruthy();
  });
});
